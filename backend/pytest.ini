[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = tests.py test_*.py *_tests.py
markers =
    throttle_test: marks tests that require real throttling (skips the global disable_throttling fixture)
